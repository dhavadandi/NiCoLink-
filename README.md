
  # Design NiCoLink Mobile UI

  This is a code bundle for Design NiCoLink Mobile UI. The original project is available at https://www.figma.com/design/QjpUsaeCNbn5zls6GVtyoa/Design-NiCoLink-Mobile-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  